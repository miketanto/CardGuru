# PHASE14-DIAG — why RL does not climb against CP7: two diagnostics (runbook + pre-registration, 2026-09-15 ~23:10Z)

Context: Phase 13c (RL vs CP7 skill 6 from the CP7 clone `bc.pt`) failed its make-or-break at 2,048 episodes
(row '13c' in rl/V7-VALIDATION.md): levels drifted below the clone, the CP7 training win rate fell 0.279 → 0.219,
the online value function explained only 0.05–0.22 of outcome variance, and updates moved the policy ~0.0012 KL
each. User (2026-09-15): a league would only drift when RL cannot climb against one fixed opponent; "go and test
those if it doesn't take long". Two diagnostics, each answering one question. Budget: ~3 h wall; D1 and D2 run
side by side (D1 = one GPU python process; D2 = one policy server + one driver JVM; box rule respected).

## D1 — can the network predict game outcomes from what it sees? (offline, GPU, ~30–60 min)

* Data: the 13a recordings `rl/artifacts/v7/13/rec/rec13_*.jsonl` (25 files, 1,250 BenchDimir games, CP7 deciding
  the recorded seat vs CP7; each game ends with `{"t":"end","r":±1}`). Every consult with a v7 state gets the
  recorded seat's final outcome as its target. Game-grouped hold-out: the same 10 % of games as 13b (`v7_bc.py`
  split, seed 0).
* Models (same parsed v7 observations as `rl/v7_bc.py`):
  - **N1** the v7 network initialised from `bc.pt`, value head (and encoder) trained on the outcome;
  - **N2** the same with the encoder frozen (what the clone's representation already holds);
  - **B0** a scalar baseline: logistic regression on simple features taken from the same consults (life me / opp,
    cards in hand me / opp, lands and creatures in play me / opp, turn) — whatever of these the observation exposes;
    list what was used.
* Readings on held-out games: explained variance of the ±1 target (1 − MSE / Var) and AUC of the sign, overall and
  by stage (own-turn buckets early ≤ 6, mid 7–12, late ≥ 13).
* Pre-registered:
  - **"The encoding can predict outcomes"** if N1's held-out EV ≥ 0.35 AND N1 beats B0 by ≥ 0.05 EV → the online
    critic is the weak link (candidates: value pre-training from recordings, a privileged-information critic).
  - **"Outcomes are barely predictable from observations"** if N1's EV ≤ 0.25 → noise from hidden information and
    shuffles (or the encoding) caps the signal; RL tuning alone cannot fix it; a privileged critic is the candidate.
  - Between: "partial", reported with the stage breakdown.
  - If B0 ≥ N1, say so: the network is not using the state better than a handful of counters.
* Cannots: states are CP7-vs-CP7 positions, not the learner's; EV depends on the stage mix; one seed.

## D2 — is CP7 skill 6 the ceiling, or is the recipe itself stuck? (GPU server + JVM, ~2–2.5 h)

* D2a (ladder, no training): `bc.pt` sampled levels, 100 games each, vs CP7 at `rl.aiSkill` 1 and 3 on BenchDimir
  (skill 6 is known: 33/100). Needs a copy of `rl/battery_p12s.sh` with the skill as a parameter (default 6; the
  original is not edited).
* Training rung: the lowest of {1, 3} where `bc.pt`'s sampled level is ≤ 0.70 (headroom); if both are > 0.70, use 3
  and say the ladder is too shallow; if skill 1 is already ≤ 0.35, use 1.
* D2b (training): from `bc.pt`, the Phase 13 recipe unchanged (lr 3e-5, 1 epoch, logit bound 5, AdamW wd 0.01 on
  heads, `--adv-norm batch`, `--target-kl 0.02`), opponent = CP7 at the rung skill only (no heuristic blocks),
  **1,024 episodes** in 256-episode blocks, then 100-game sampled and two-stage levels vs the rung skill.
* Pre-registered:
  - **"Climbs against a weaker CP7"** if the sampled or two-stage level at 1,024 is clearly above `bc.pt`'s level
    at that skill (Wilson intervals do not overlap), OR the pooled training win rate of blocks 512→1,024 is clearly
    above blocks 0→512 → skill 6's search is (part of) the ceiling; lookahead or a curriculum are the candidates.
  - **"Does not climb even against a weaker CP7"** otherwise → the learning signal / recipe is the block regardless
    of opponent strength; D1 says which part.
* Cannots: one seed; 1,024 episodes is short (a "does not climb" here is about this recipe at this scale); CP7's
  skill scale is not linear.

## Combined reading (stated before either runs)

| D1 | D2 | reading |
|---|---|---|
| predicts | climbs vs weaker | the critic and opponent strength both matter: pre-train the critic, then a CP7 curriculum |
| predicts | does not climb | the online critic / update is the block: value pre-training, larger steps with a KL-to-bc.pt anchor |
| barely predicts | climbs vs weaker | the signal is noisy but usable against weaker play: curriculum + privileged critic |
| barely predicts | does not climb | the win/loss signal from this encoding is too weak for this RL at this scale: privileged critic, denser rewards, or search |

## STATE (append-only)
- 2026-09-15 23:16Z WSL: 13c has exited (L13|done|reason=stopfile, recorded in rl/PHASE13-BC.md STATE); box free (no server, no driver JVM; three old `sleep 64800` keepalives left alone). D1 and D2 both start now.
- 2026-09-15 23:18Z WSL (D2a LAUNCHED): rl/battery_p14s.sh = copy of rl/battery_p12s.sh with `SKILL` (default 6) in `-Drl.aiSkill` and `skill=` in the XDECKS line (diff: 3 lines + header; the original is untouched); rl/battery_x14.sh = the same change on rl/battery_xdeck.sh (for the two-stage levels). Runner rl/run_14d2a.sh (cmd start /min wsl): bc.pt sampled, 100 games vs CP7 on BenchDimir, row seed 930000 (= 13b's skill-6 level), skill 1 on 7949/7914 and skill 3 on 7947/7913 in parallel. Output rl/artifacts/v7/14/d2a/run.log (XDECKS|...|skill=N| lines), probes in d2a/s1, d2a/s3. pgrep-verified 23:18:50Z (runner, server 7949, JVM).
- 2026-09-15 23:24Z WSL (D1 written, HELD until D2a finishes - memory, not a change of plan): rl/p14_d1.py (N1 = bc.pt's critic trained on the outcome; N2 = a value MLP on the FROZEN clone game vector, computed from an untouched load of bc.pt BEFORE N1 trains, because the critic shares the card-table adapter with the policy path; B0 = logistic regression on life/hand/lands/non-land-permanents me+opp, turn, my-turn flag read from v7_game / v7_players). Hold-out = the 13b split reproduced exactly (labelled games, sorted, Random(0).shuffle, first 10 %); early stopping uses a validation split of 10 % of the TRAINING games (Random(1)), so the hold-out is never used for model selection - a deviation from v7_bc.py, which early-stops on its hold-out. 95 % intervals = 1,000 bootstrap resamples of held-out GAMES. Smoke (1 file, 50 games) OK end to end; 3-file probe gives RSS ~420 MB + ~62 MB/file, so 25 files is ~2.0 GB. With D2a resident (2 servers + 2 JVMs) MemAvailable is 3.4 GB, so D1 starts when D2a finishes and runs beside D2b's lane (1 server + 1 JVM) instead: the box rule's 1.5 GB floor decides the order, the runbook's content is unchanged.
- 2026-09-15 23:33Z WSL (D2a RESULT + rung): bc.pt sampled, 100 games, BenchDimir mirror, row seed 930000 (= 13b's skill-6 row), 0 stalls in both rows. CP7 skill 1: 23/100 = 0.230 [0.158, 0.322], 18.8 turns/ep. CP7 skill 3: 18/100 = 0.180 [0.117, 0.267], 18.7 turns/ep. Known skill 6 (13b): 33/100 = 0.330 [0.246, 0.427]. The ladder is INVERTED: the clone does not do better against weaker CP7, it does slightly worse (all three Wilson intervals overlap; skill 3 vs skill 6 only just). RUNG = skill 1 by the pre-registered rule as written ("if skill 1 is already <= 0.35, use 1"; it is also the lowest of {1, 3} at <= 0.70). Stated plainly: the rule's premise - a weaker CP7 gives the clone headroom - is contradicted by the measurement, and no threshold is changed after seeing it. Artifacts rl/artifacts/v7/14/d2a/ (run.log, s1/, s3/).
- 2026-09-15 23:34Z WSL (D2b + D1 LAUNCHED together, box = lane 1 server + 1 JVM, plus D1 ~2 GB): D2b `SKILL=1 bash rl/run_14d2b.sh` (cmd start /min wsl; log /tmp/d2b.log, D2B| lines in rl/artifacts/v7/14/d2b/run.log, lane /tmp/rl_14_d2b/lane.log, lane seed 41, ports 7950/7960/7912): agent.pt from bc.pt (opt popped), 4 x 256 episodes vs CP7 skill 1 only, Phase 13 recipe unchanged, then 100-game sampled (7949/7914) and two-stage (7947/7913) levels at skill 1. D1 `python3 rl/p14_d1.py` over all 25 recordings -> rl/artifacts/v7/14/d1/d1.log + d1.json. pgrep verification next call.
- 2026-09-15 23:37Z WSL (what `rl.aiSkill` ACTUALLY maps to - checked in the engine source, coordinator's question): `-Drl.aiSkill` is passed straight into the CP7 constructor (`rl/xmage-src/EpisodeRunner.java:248-252` for the opponent seat, `:178-181` for the teacher seat), `ComputerPlayer7(String, RangeOfInfluence, int skill)` forwards it unchanged to its parent (`/home/user/mage/Mage.Server.Plugins/Mage.Player.AI.MAD/src/mage/player/ai/ComputerPlayer7.java:23-24`), and the parent binds it (`.../ComputerPlayer6.java:92-101`):
      if (skill < 4) { maxDepth = 4; } else { maxDepth = skill; }
      maxThinkTimeSecs = skill * 3;
      maxNodes = MAX_SIMULATED_NODES_PER_CALC;          // 5000, a constant (ComputerPlayer6.java:54)
  So: skill 1 -> depth 4, think 3 s; skill 3 -> depth 4, think 9 s; skill 6 -> depth 6, think 18 s. The node cap is the same at every skill, and nothing about evaluation or candidate pruning changes with skill. TWO consequences for D2a: (i) skill 1 and skill 3 have the SAME search depth and differ only in think time, so the ladder's lower two rungs are a TIME ladder, not a depth ladder - which is consistent with skill 3 (more time, same depth) beating skill 1; (ii) the search is WALL-CLOCK limited, so CP7's effective strength depends on machine load. The D2a arms ran in parallel (2 servers + 2 JVMs competing), while the reference skill-6 row (13b) ran alone - load would have made the skill-1 / skill-3 opponents WEAKER than their nominal budget, i.e. the confound pushes the opposite way from the observed inversion, so it cannot explain it away.
- 2026-09-15 23:37Z WSL (D2a READING, and a caveat PRE-STATED before D2b's result - nothing here is a threshold change): the ladder is inverted (0.230 at skill 1, 0.180 at skill 3, 0.330 at skill 6). What it supports: the clone was cloned from CP7 AT SKILL 6 (13a recorded `rl.agent=cp7 -Drl.aiSkill=6`), so a lower-skill CP7 is an OFF-DISTRIBUTION opponent - a differently-behaving one - rather than a weaker version of the opponent bc.pt was trained to imitate; a shallower, less deliberative CP7 is not easier for this clone. What it cannot support: the three intervals overlap (skill 1 [0.158,0.322] and skill 3 [0.117,0.267] overlap heavily; skill 1 and skill 6 [0.246,0.427] overlap slightly), so the ORDER of the rungs is not established by 100 games each - only "no headroom appears at lower skill" is; one seed, one deck, sampled readout only.
  PRE-STATED (before D2b reports): because the ladder is inverted, a "does not climb" at skill 1 CANNOT be read as "the recipe cannot climb against a weaker opponent". It can only say: this recipe, from this start, does not climb against THIS off-distribution opponent in 1,024 episodes. The runbook's combined-reading table is entered with that restriction attached to the D2 column. Also pre-stated: CP7 at skill 1 is wall-clock-limited (3 s), the training lane runs 4 concurrent driver jobs (R0_CONC=4) while the eval rows run 1-2, so the opponent the learner trains against and the one it is scored against are not guaranteed to be equally strong; that is a confound of D2b, stated in advance and not measurable from these runs.
- 2026-09-15 23:46Z WSL (D1 DONE, 689 s): reading **barely_predicts** — N1 held-out EV 0.095 [-0.172,0.256] / AUC 0.788, N2 0.146 / 0.761, B0 0.196 [0.002,0.315] / 0.799; N1-B0 = -0.101 [-0.293,+0.074], so B0 >= N1 and the "can predict" clause (EV >= 0.35 AND N1-B0 >= 0.05) fails on both halves. By stage (own turn): early ~0 for all three, mid ~0.34-0.38 for all three, late 481 consults over 17 games with useless intervals. N1 best epoch 2 of 12 (overfits: train 0.568->0.235 while val 0.560->0.731). Row written to rl/V7-VALIDATION.md '14 / D1'. Artifacts rl/artifacts/v7/14/d1/{d1.log,d1.json}.
- 2026-09-15 23:47Z WSL (bc.pt two-stage baseline DEFERRED, box rule): D1 exited and freed ~3.3 GB (MemAvailable 4.3 GB), but a battery arm (frozen server + driver JVM) measured ~4.7 GB during D2a and the D2b lane pair is holding ~5.7 GB, so launching rl/run_14bcts.sh now would exceed available memory and could OOM a training block. It runs after D2b's training blocks instead. Wall-clock has room: start 23:16Z, projected finish ~01:05Z against the ~3 h budget.
- 2026-09-15 23:48Z WSL (TIMING, flagged as the brief requires): D2b block 1 has 2 of 4 chunks done (128 of 256 episodes) in ~14 min, so ~28 min per 256-episode block at CP7 skill 1. Projection: 4 blocks end ~01:25Z, the two 100-game levels ~01:40Z, the deferred bc.pt two-stage baseline ~01:55Z - about **2 h 40 m** from the 23:16Z start, above the ~2.5 h guidance in the brief. Nothing is trimmed: the brief says to say so and still finish, and every remaining step is pre-registered. The only elective item is the bc.pt two-stage baseline row, which exists because D2a measured bc.pt sampled only and D2b's two-stage level needs a same-skill comparison; it is sequenced last so the box never exceeds 2 servers + 2 JVMs (a battery arm measured ~4.7 GB during D2a, and 5.9 GB free minus that leaves ~1.2 GB, under the 1.5 GB floor).
- 2026-09-16 00:46Z WSL (D2b DONE, 1,024 episodes vs CP7 skill 1 from bc.pt, lane seed 41): blocks 69/256 = 0.270 (1118 s), 67/256 = 0.262 (791 s), 62/256 = 0.242 (857 s), 81/256 = 0.316 (849 s); 0 draws, 0 stalls, rc=0 throughout. Pooled halves: 0->512 136/512 = 0.266 [0.229,0.306], 512->1,024 143/512 = 0.279 [0.242,0.320] - rule (b) ("512->1,024 clearly above 0->512") NOT met, the intervals overlap almost completely. Whole run 279/1024 = 0.272 [0.246,0.301]. Levels of ck_1024.pt at skill 1: sampled 34/100 = 0.340 [0.255,0.437] (18.9 turns), two-stage 33/100 = 0.330 [0.246,0.427] (18.4 turns), 0 stalls. bc.pt at skill 1 (D2a): sampled 23/100 = 0.230 [0.158,0.322] - the point estimate rises 0.230 -> 0.340 but the Wilson intervals OVERLAP, so rule (a) is not met on the sampled readout. The two-stage half of rule (a) needs bc.pt's two-stage at skill 1, LAUNCHED 00:46:47Z (rl/run_14bcts.sh, ports 7946/7915, box empty: 11.4 GB free). Contrast worth keeping: 13c at skill 6 went 0.330 -> 0.220 sampled over its first 1,024 episodes, while here at skill 1 it goes 0.230 -> 0.340; the CP7 training win rate is ~the same at both skills (13c 0->1,024 = 0.279; here 0.266 / 0.279).
- 2026-09-16 00:55Z WSL (bc.pt TWO-STAGE baseline at skill 1, the last measurement): 38/100 = 0.380 [0.291,0.478], 18.1 turns, 0 stalls (rl/run_14bcts.sh, 00:46:48Z -> 00:55:28Z; rl/artifacts/v7/14/d2a/bc_twostage.log). So ck_1024's two-stage 0.330 [0.246,0.427] is BELOW its start, while sampled rose 0.230 -> 0.340: the two readouts disagree in DIRECTION and both are reported. bc.pt's own readouts differ by 0.15 at skill 1 (0.230 vs 0.380) against near-agreement at skill 6 (0.330 vs 0.310) - at this opponent the readout choice moves the number more than the training did.
- 2026-09-16 00:56Z WSL (PHASE 14 CLOSED): D2b reading = "does not climb even against a weaker CP7" (rule (a) fails on both readouts, rule (b) fails), carrying the pre-stated caveat that skill 1 is off-distribution rather than weaker. D1 reading = "outcomes are barely predictable". COMBINED (runbook table): barely predicts x does not climb -> "the win/loss signal from this encoding is too weak for this RL at this scale: a privileged critic, denser rewards, or search." Section '## 14 - diagnostics' in rl/V7-VALIDATION.md is complete (D2a ladder + engine finding, D1 table + addendum, D2b tables, combined reading, cannots). Box empty at close: 0 servers, 0 driver JVMs, 11.4 GB free; the three old `sleep 64800` keepalives from 13c are still resident and were never touched. Total wall 23:16Z -> 00:56Z = 1 h 40 m, under the ~3 h budget and under the ~2.5 h the 23:48Z projection feared (D2b's later blocks ran ~850 s, not the ~1,120 s of block 1).
